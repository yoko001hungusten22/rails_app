<div class="top">
  <p> ユーザー詳細ページ</p>
  <%= render @user %>
<% if current_user == @user %>
  <p><%= link_to "ユーザー情報を編集", edit_user_path%></p>
<% end %>
<p><%= link_to "削除", @user, method: :delete, data: { confirm: "削除していいですか？"} %></p>
</div>